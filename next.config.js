/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add any other configurations here, e.g., image domains if using next/image
};

module.exports = nextConfig;
